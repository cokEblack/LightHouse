package breakout.game;

public class Health extends RegenerativeGameObjectResource {

    public Health(float currentValue, float maxValue, float regenerationRate) {
        super(currentValue, maxValue, regenerationRate);
    }

}
